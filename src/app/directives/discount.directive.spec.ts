import { DiscountDirective } from './discount.directive';

describe('DiscountDirective', () => {
  it('should create an instance', () => {
    const directive = new DiscountDirective();
    expect(directive).toBeTruthy();
  });
});
